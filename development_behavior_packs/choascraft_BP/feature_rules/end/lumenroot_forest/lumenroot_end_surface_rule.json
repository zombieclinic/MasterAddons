{
  "format_version": "1.13.0",
  "minecraft:feature_rules": {
    "description": {
      "identifier": "zombie:lumenroot_end_surface_rule",
      "places_feature": "zombie:lumenroot_end_surface"
    },
    "conditions": {
      "minecraft:biome_filter": [
        {
          "test": "has_biome_tag",
          "operator": "==",
          "value": "the_end"
        }
      ],
      "placement_pass": "surface_pass"
    },
    "distribution": {
      "iterations": 256,
      "coordinate_eval_order": "xzy",
      "x": {
        "distribution": "fixed_grid",
        "extent": [0, 15]
      },
      "z": {
        "distribution": "fixed_grid",
        "extent": [0, 15]
      },
      "y": "((q.noise(v.worldx / 512, v.worldz / 512) + q.noise(v.worldx, v.worldz) * 0.05) >= 0.35 && (q.noise(v.worldx / 512, v.worldz / 512) + q.noise(v.worldx, v.worldz) * 0.05) <= 0.85 && (math.abs(v.worldx) > 200 || math.abs(v.worldz) > 200)) ? (q.heightmap(v.worldx, v.worldz) - 1) : -1"
    }
  }
}
